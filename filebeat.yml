filebeat.inputs:
- type: container
  paths:
    - '/var/lib/docker/containers/*/*.log' # Он сам найдет логи всех контейнеров!

output.elasticsearch:
  hosts: ["elasticsearch:9200"]
  indices:
    - index: "filebeat-%{[agent.version]}-%{+yyyy.MM.dd}"